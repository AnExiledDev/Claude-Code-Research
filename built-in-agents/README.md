Each agent file starts with metadata, as captured from the obfuscated Claude Code CLI source directly, including which version of Claude Code it was taken from.

Everything below the metadata separator is defined as that Agents System Prompt.